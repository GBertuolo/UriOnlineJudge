#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main() {
    int tempo, velocidade;
    double consumo;
    scanf("%d", &tempo);
    scanf("%d", &velocidade);
    consumo = tempo * velocidade / 12.000;
    printf("%.3lf\n", consumo);
    return 0;
}
